# agent_specs

This folder is reserved for shared specification repositories used by
`AGENT.md`.

Expected path for the self-standing example:

- `agent_specs/DevSpec/DevSpecs.md` (DevSpec repository, typically as a submodule)
